#include <stdio.h>
void main(){
    int r,c;
    printf("enter the value of r and c");
    scanf("%d%d",&r,&c);
    int a[r][c];
    printf("enter %d values \n",c);
    for  (int i=0;i<r;i++){
        for(int j=0;j<c;j++){
        printf("%d",a[r][c]);
        }
        printf("\n");
    }
}
