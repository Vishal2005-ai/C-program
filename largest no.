#include <stdio.h>

void main()
{
    int a,b;
    printf("enter any 2 no.");
    scanf("%d%d",&a,&b);
    {
        if(a>b)
        printf("the %d is a largest no.", a);
        else 
        printf("the %d is a largest no.", b);
    }
    
 
}
